export type FormField = {
    id: number,
    name: string,
    value: number | Date
}
export type LoanDetails = {
    principal: number,
    interestRate: number,
    tenure: number,
    startDate:  Date;
}
